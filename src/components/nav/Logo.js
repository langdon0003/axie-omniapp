import { Box, Text } from "@chakra-ui/react"

const Logo = (props) => {
    return (
        <Box {...props}>
            <Text fontSize="2xl" fontWeight="bold" textAlign="center">Axie Infinity</Text>
        </Box>
    )
}

export default Logo